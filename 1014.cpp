
#include <bits/stdc++.h>
using namespace std;

namespace p1014 {
enum { maxt = 5400 };
enum { maxk = 1005 };
enum { maxn = 25 };
int N, M, K, Q;
map<int, int> times[maxk];
queue<int> outWaiting;
deque<int> inWaiting[maxn];
int main() {
  cin >> N >> M >> K >> Q;
  for (int i = 0; i < K; ++i) {
  }
  for (int i = 0; i < Q; ++i) {
  }
  return 0;
}
} // namespace p1014

int main() { return p1014::main(), 0; }
